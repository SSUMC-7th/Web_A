import styled from "styled-components";

const Search = () => {
    return (
        <SearchText>검색페이지에요</SearchText>
    );
};

export default Search;

const SearchText = styled.h1`
    color:white;
`